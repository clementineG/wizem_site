{% extends '::base.html.twig' %}

{% block body -%}

    <h1>Dashboard User {{app.user.username}}</h1>
    
    
    <a class="waves-effect waves-light btn" href="{{ path('wizem_event_event') }}">
        Mes évènements
    </a>

{% endblock %}